// gap loop very handy in 2D Dynamic Programming Problems.
for(int gap=1;gap<n;gap++){
		for(int i=0,j=gap;i+gap<n;i++,j++){
		// NOW, (i,j) can be handled as simple iterators for getting to different positions.
		}
	}

// Square loop for DP

for(int i=0;i<n;i++){
  for(int j=0;j<n;j++){
    // NOW, (i,j) can be handled as simple iterators for getting to different positions.   
  }
}
